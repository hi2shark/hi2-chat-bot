# ===========================================
# TG私聊机器人环境变量配置示例
# ===========================================
# 使用方法：
# 1. 复制此文件为 .env: cp env.example .env
# 2. 填写必填配置项
# 3. 根据需要配置可选项
# ===========================================

# ===========================================
# 必填配置
# ===========================================

# Telegram机器人Token（从 @BotFather 获取）
TELEGRAM_BOT_TOKEN=

# 您的ChatId（从 @userinfobot 获取，也可以是群组ID）
MY_CHAT_ID=

# ===========================================
# MongoDB配置
# ===========================================

MONGODB_URL=mongodb://mongodb:27017
MONGODB_NAME=hi2chatbot

# ===========================================
# 基础功能配置（可选）
# ===========================================

# 是否允许编辑消息（0=不允许，1=允许）
ALLOW_EDIT=0

# 自动清除消息关系记录的时间（单位：小时，默认720小时=30天）
MESSAGE_CLEAR_HOURS=720

# 隐藏启动消息（1=隐藏，0或留空=不隐藏）
HIDE_START_MESSAGE=0

# 时区设置
TZ=Asia/Hong_Kong

# ===========================================
# AI审核配置（可选）
# ===========================================

# 是否启用AI审核功能（1=启用，0=不启用）
AI_AUDIT_ENABLED=0

# 需要审核的消息次数（新用户前N条消息）
AI_AUDIT_COUNT=1

# OpenAI API密钥（启用AI审核时必填）
OPENAI_API_KEY=

# OpenAI API基础URL（支持自定义，如使用代理或其他兼容接口）
OPENAI_BASE_URL=https://api.openai.com/v1

# 使用的AI模型名称
OPENAI_MODEL=gpt-3.5-turbo

# AI审核提示词文件路径（为空则使用默认提示词）
AI_AUDIT_PROMPT_FILE=/app/ai-audit-prompt.txt

# 是否通知被AI拉黑的用户（1=通知，0=不通知）
AI_AUDIT_NOTIFY_USER=0

# ===========================================
# 日志配置（可选）
# ===========================================

# 日志文件路径（为空则只输出到控制台）
LOG_FILE_PATH=/app/logs/chatbot.log

# 日志文件最大大小（单位：MB，默认10MB）
# 超过此大小时，当前日志文件会被重命名为 .log.1，并创建新的日志文件
LOG_MAX_SIZE=10

# ===========================================
# 监控配置（可选）
# ===========================================

# Uptime Kuma推送URL
# UPTIME_KUMA_PUSH_URL=

